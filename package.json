{
  "name": "wsiox",
  "version": "1.5.1",
  "description": "websocket request lib, like axios",
  "main": "dist/Wsiox.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " webpack --config webpack.config.prod.js",
    "dev": " webpack --config webpack.config.dev.js",
    "patch:pub": "npm version patch && npm run build && npm publish",
    "minor:pub": "npm version minor && npm run build && npm publish",
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "author": "cookingBird",
  "license": "MIT",
  "typings": "types/index.d.ts",
  "devDependencies": {
    "@babel/core": "^7.20.2",
    "@babel/preset-env": "^7.20.2",
    "babel-loader": "^9.1.0",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-merge": "^5.8.0"
  }
}
